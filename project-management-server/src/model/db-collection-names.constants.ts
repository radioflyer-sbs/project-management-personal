/** Central registry of MongoDB collection name strings.
 *  Import this constant everywhere — never use inline string literals for collection names. */
export const DbCollectionNames = {
    // TODO-Immediate: Add collection names for your domain entities.
    // Example:
    // Projects: 'projects',
} as const;
