/** Position and size of an item within its parent canvas (canvas-space units). */
export interface Layout {
    x: number;
    y: number;
    width: number;
    height: number;
    zIndex: number;
}
