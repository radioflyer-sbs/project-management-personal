/** Task urgency. Drives card color only — no mechanical behavior (D7). */
export enum TaskUrgency {
    LongTermGoal = 'long-term-goal',
    Low = 'low',
    Normal = 'normal',
    Important = 'important',
    Urgent = 'urgent',
    Immediate = 'immediate',
}
